import { Home, MoreDetails } from '@/features';
import React from 'react';

const Index = () => {
    return (
        <React.Fragment>
            <Home />
            <MoreDetails />
        </React.Fragment>
    );
}

export default Index;